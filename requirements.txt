# RAG-Mungil standard library requirements
# Crawler & data pipelines intentionally use Python standard library (urllib, re, json, hashlib)
# to keep GitHub Actions execution instant, zero-dependency, and bloat-free.
